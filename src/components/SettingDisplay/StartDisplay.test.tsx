import React from 'react';
import { render, screen } from '@testing-library/react';
import StartDisplay from '.';

/* test('Текст присуствует "Поехали"', () => {
    render(<StartDisplay />);
    const linkElement = screen.getByText(/Поехали/i);
    expect(linkElement).toBeInTheDocument();
}); */

/* test('Текст присутствует "Максимальное число"', () => {
    render(<StartDisplay />);
    const linkElement = screen.getByText(/Максимальное число/i);
    expect(linkElement).toBeInTheDocument();
}); */